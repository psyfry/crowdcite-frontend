import React from 'react'

const Login = ({
    handleLogin,
    handleUsername,
    handlePassword,
    username,
    password
}) => {
    return <div className='loginForm'>Login</div>
}

export default Login
